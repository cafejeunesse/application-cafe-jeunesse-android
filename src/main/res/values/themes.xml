<resources>

    <style name="AppTheme" parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <!-- Style de l'actionBar -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- Suppression de l'ombre sur l'actionBar -->
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.TranslucentActionBar">
        <item name="android:actionBarStyle">@style/Widget.ActionBar.Transparent</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.Light.TranslucentActionBar" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/Widget.Light.ActionBar.Transparent</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:style/Widget.ActionBar">
        <!-- Background de l'actionBar -->
        <item name="android:background">@color/fragment_rectangle</item>
        <!-- Suppression des titres l'actionBar -->
        <item name="android:displayOptions">useLogo|showHome</item>
        <!-- Icone custom pour l'actionBar -->
        <item name="android:icon">@drawable/actionbar_icon</item>
    </style>

</resources>